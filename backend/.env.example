# ─────────────────────────────────────────────────────────────
# GeoVerify Logistics — Backend Environment Variables
# Copy file ini menjadi .env dan isi nilai yang sesuai.
# JANGAN commit file .env ke Git!
# ─────────────────────────────────────────────────────────────

# ── App ──────────────────────────────────────────────────────
APP_ENV=development
APP_PORT=8081

# ── Database (PostgreSQL + PostGIS) ──────────────────────────
# Development lokal:
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=geodata

# Production (Supabase) — uncomment dan isi:
# DATABASE_URL=postgresql://postgres:[PASSWORD]@db.[PROJECT].supabase.co:5432/postgres

# ── Redis ────────────────────────────────────────────────────
# Development lokal:
REDIS_URL=redis://localhost:6379

# Production (Upstash Redis gratis) — uncomment dan isi:
# REDIS_URL=rediss://default:[TOKEN]@[HOST].upstash.io:6379

# ── Security ─────────────────────────────────────────────────
# Generate: openssl rand -hex 32
JWT_SECRET=super-secret-default-key-change-in-prod

# HARUS tepat 32 karakter untuk AES-256
AES_ENCRYPTION_KEY=0123456789abcdef0123456789abcdef

# ── CORS ─────────────────────────────────────────────────────
# Daftar origin yang diizinkan, pisahkan dengan koma (tanpa spasi).
#
# Development — izinkan Vite dev server:
ALLOWED_ORIGINS=http://localhost:8080,http://localhost:5173
#
# Production (FE di Vercel) — ganti dengan domain Vercel Anda:
# ALLOWED_ORIGINS=https://nama-project.vercel.app,https://custom-domain.com
