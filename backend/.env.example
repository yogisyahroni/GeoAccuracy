# ============================================================
# GeoVerify Logistics - Backend Environment Variables
# Copy to .env dan isi semua nilai yang diperlukan
# ============================================================

APP_ENV=production
APP_PORT=8081

# ── Database: Supabase PostgreSQL ─────────────────────────────
# Ambil dari: https://supabase.com/dashboard/project/<id>/settings/database
DB_HOST=db.<PROJECT_REF>.supabase.co
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=Namakamu766!!
DB_NAME=postgres

# ── Optional: Supabase Connection Pooler (high-traffic) ───────
# Mode: Transaction (port 6543), Session (port 5432)
# DB_HOST=<region>.pooler.supabase.com
# DB_PORT=6543
# DB_USER=postgres.<PROJECT_REF>

# ── JWT Authentication ─────────────────────────────────────────
JWT_SECRET=GENERATE_RANDOM_64_CHAR_SECRET_HERE

# ── Encryption (AES-256) ──────────────────────────────────────
# HARUS 32 karakter tepat (untuk AES-256)
AES_ENCRYPTION_KEY=GANTI_DENGAN_32_KARAKTER_KEY_ANDA

# ── CORS: Frontend URLs yang diizinkan ───────────────────────
# Pisahkan dengan koma untuk multiple origins
ALLOWED_ORIGINS=https://your-frontend-domain.com,http://localhost:5173

# ── Redis (untuk rate limiting & caching) ─────────────────────
REDIS_URL=redis://localhost:6379

# ── Supabase Info (referensi) ─────────────────────────────────
# Project URL: https://<PROJECT_REF>.supabase.co
# Project Ref: odawdxitezoivptffnsy (contoh)
